# tools.yaml — stc2557_adk_s3s4s5
#
# Windows ADK 每次都執行 reinstall，確保乾淨的 baseline。
# smicli 用於 RunCard 的 DUT 資訊收集（generate_dut_info）。
# version 未填寫 → 自動使用 package_meta.yaml 中 default: true 的版本

tools:
  - id: windows-adk
    reinstall: true

  - id: smicli
    reinstall: false
