# tools.yaml — stc2557_adk_s3s4s5
#
# phase: pre_runcard — 在 setup_test_class 中、RunCard 初始化前安裝
# phase: test        — 在 test_01_precondition 中安裝（預設，可省略）
#
# env — 手動覆蓋 env var（通常不需要；ToolInstaller 會自動從 package_meta.yaml 注入）

tools:
  - id: smicli
    reinstall: false
    phase: pre_runcard    # RunCard 需要 SmiCli2.exe 來收集 DUT 資訊

  - id: windows-adk
    reinstall: false

  - id: cdi
    reinstall: false
